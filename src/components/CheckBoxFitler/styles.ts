import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

    headerText: {
        color: "#ffffff",
        
    },
    container: {
       
        flexDirection: 'row'
        
    }
});

export default styles;